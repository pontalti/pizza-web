# pizza-web
Spring Boot - MVC web application + bootstrap + jquery
Use the address bellow to test Spring MVC
http://localhost:8080

To test the rest methods to get the list of pizza use the addresses below
localhost:8080/pizza
localhost:8080/pizza/{name}
